Dial and busy sounds can now be turned off permanently in settings.
Making calls can now be done in complete silence.

When login cookie is cleared, the client will now switch back 
to the basepage. This allows for quicker ID switching.

Opening a WebCall link in another app, is now possible if
WebCall was not yet started / is not currently running.

