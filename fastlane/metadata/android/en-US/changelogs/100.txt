v1.1.0

added runJS('offlineAction();',null) under 'No network. Giving up.'

v1.0.8

new JavascriptInterface reload() for 'Clear cache' at runtime

if login-from-boot (onStartCommand) -> set connectToSignalingServerIsWanted=true

