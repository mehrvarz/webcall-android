ID mapping

Intent-filter VIEW and NDEF_DISCOVERED not tight to specific host anymore.
WebCall links + NFC-connect now work with hostnames other than timur.mobi.

New menu item 'Clear cache' can be invoked at any time to updated to the
latest core client. No need to exit and restart app anymore.

WebCall update can now be shown in-app. No need to open external browser.

Fix: Correct button state in response to status 'No network. Giving up.'

Fix: Reconnect stability after service was auto-started from boot.

Fix: Go offline will immediately stop service reconnecter.

