ID mapping

Intent-filter VIEW and NDEF_DISCOVERED not tight to specific host anymore
webcall links and NFC-connect now work with other hosts than timur.mobi

New menu item 'Clear cache' can be invoked at any time to updated to latest
core client. No more need to exit and restart app.

WebCall news is now shown in-app. No need to open external browser anymore.

Fixed: to ensure correct button state, runJS('offlineAction();') is now called
in response to status 'No network. Giving up.'

Fixed: an issue with reconnect stability when service was auto-started from boot

Fixed: service reconnecter run(): added 2x if(!connectToSignalingServerIsWanted) abort

