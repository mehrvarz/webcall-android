WebCall links can now be opened with the WebCall app from within 
3rd party apps, even if the WebCall app is not currently running.

Dial and busy sounds can be turned off permanently in settings.
Calls can now be established in complete silenc<e.

When the login-cookie is cleared, WebCall app will now switch back
to the basepage. This allows for quicker ID switching.

Basepage login now checks for 'account busy' and will in this
case display: 'Busy. Already logged in from another device?'

bugfix: service reconnecter: new insecureTlsFlag handling for
HttpsURLConnection re-login while in background.

bugfix: On long duration connection loss, screen will not light up
unless 'Screen for Wifi' is enabled.

