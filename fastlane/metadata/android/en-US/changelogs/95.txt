WebCall links can now be opened with the WebCall app from within 
3rd party apps, even if the WebCall app is not currently running.

Dial and busy sounds can be switched off permanently in settings.
Calls can now be established in complete silenc<e.

When the login-cookie is cleared, WebCall app will now switch back
to the basepage. This allows for quicker ID switching.

Basepage login now checks for 'Busy. Already logged in from another
device'

On long duration connection loss, screen will only light up if
'Screen for Wifi' is enabled.

